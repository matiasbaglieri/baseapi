# MySQL Configuration
MYSQL_USER=root
MYSQL_PASSWORD=toor
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=baseapi

# CORS Configuration
CORS_ORIGINS='["http://localhost:3000", "http://localhost:8000"]'
CORS_CREDENTIALS=true
CORS_METHODS='["GET", "POST", "PUT", "DELETE", "OPTIONS"]'
CORS_HEADERS='["Content-Type", "sessionId", "Authorization", "Access-Control-Allow-Methods", "Access-Control-Allow-Origin", "Access-Control-Request-Headers"]'

# Server Configuration
HOST=0.0.0.0
PORT=8000
# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

#jwt implementatio
JWT_SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
JWT_ALGORITHM=HS256
JWT_ISSUER=baseapi
JWT_AUDIENCE=baseapi
JWT_LEEWAY=10
ACCESS_TOKEN_EXPIRE_MINUTES=300000
REFRESH_TOKEN_EXPIRE_DAYS=15

# Email settings
SMTP_TLS=true
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
EMAILS_FROM_EMAIL=your-email@gmail.com
EMAILS_FROM_NAME=BaseAPI

# Session settings
SESSION_EXPIRY_DAYS=30

# Security settings
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7
BACKEND_CORS_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# API settings
PROJECT_NAME=BaseAPI

# Frontend settings
FRONTEND_URL=http://localhost:3000

# Development settings
DEV_MODE=true  # Set to false in production

# Mailgun Configuration (required in production)
# Get these values from https://app.mailgun.com/app/account/security/api_keys
MAILGUN_API_KEY=your-api-key-here        # Replace with your actual Mailgun API key
MAILGUN_DOMAIN=your-domain.com           # Replace with your verified Mailgun domain
MAILGUN_FROM_EMAIL=noreply@your-domain.com  # Replace with your sender email